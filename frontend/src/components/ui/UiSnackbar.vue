<template><Teleport to="body"><Transition name="ui-snackbar"><div v-if="show" class="ui-snackbar" :class="`ui-snackbar--${tone}`" role="status"><Icon :name="tone==='success'?'checkCircle':tone==='danger'?'exclamationCircle':'infoCircle'" size="sm"/><span>{{ message }}</span><button v-if="action" type="button" @click="emit('action')">{{ action }}</button><UiIconButton label="关闭" variant="ghost" density="mini" @click="emit('close')"><Icon name="x" size="xs"/></UiIconButton></div></Transition></Teleport></template>
<script setup lang="ts">import Icon from'@/components/icons/Icon.vue';import UiIconButton from'./UiIconButton.vue';withDefaults(defineProps<{show:boolean;message:string;action?:string;tone?:'neutral'|'success'|'danger'}>(),{tone:'neutral'});const emit=defineEmits<{close:[];action:[]}>();</script>
<style scoped>.ui-snackbar{position:fixed;z-index:100000020;bottom:calc(20px + env(safe-area-inset-bottom));left:50%;display:flex;min-height:40px;align-items:center;gap:8px;max-width:calc(100vw - 32px);padding:6px 8px 6px 12px;border-radius:6px;color:#fff;background:#1f2329;box-shadow:0 8px 24px rgb(0 0 0/.14);transform:translateX(-50%)}.ui-snackbar--success>svg{color:var(--ui-success)}.ui-snackbar--danger>svg{color:var(--ui-danger)}.ui-snackbar>button:not(.ui-icon-button){border:0;color:#fff;background:transparent;font-weight:600}.ui-snackbar-enter-active,.ui-snackbar-leave-active{transition:opacity var(--ui-motion-base),transform var(--ui-motion-base) var(--ui-ease-enter)}.ui-snackbar-enter-from,.ui-snackbar-leave-to{opacity:0;transform:translate(-50%,8px)}</style>
