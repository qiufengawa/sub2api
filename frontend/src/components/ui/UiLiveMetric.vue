<template><div class="ui-live"><header><span>{{label}}</span><UiPulseIndicator :tone="stale?'warning':'success'" :animated="live&&!stale"/><small>{{stale?'数据延迟':live?'实时':'已暂停'}}</small></header><strong class="ui-numeric">{{value}}<small v-if="unit">{{unit}}</small></strong><footer><span v-if="updatedAt">{{updatedAt}}</span><slot name="context"/></footer></div></template>
<script setup lang="ts">import UiPulseIndicator from './UiPulseIndicator.vue';withDefaults(defineProps<{label:string;value:string|number;unit?:string;live?:boolean;stale?:boolean;updatedAt?:string}>(),{live:true,stale:false});</script>
<style scoped>.ui-live{display:grid;gap:5px;min-width:0;padding:10px;border:1px solid var(--ui-border-soft);border-radius:var(--ui-radius);background:var(--ui-surface)}.ui-live header,.ui-live footer{display:flex;align-items:center;gap:6px;color:var(--ui-text-soft);font-size:10px}.ui-live header>span{margin-right:auto;color:var(--ui-text-muted);font-size:12px}.ui-live>strong{font-size:22px;font-weight:500}.ui-live>strong small{margin-left:4px;color:var(--ui-text-soft);font-size:10px}.ui-live footer{justify-content:space-between}</style>
