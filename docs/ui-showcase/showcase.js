document.addEventListener('DOMContentLoaded',()=>{if(window.lucide)window.lucide.createIcons({attrs:{'stroke-width':1.75}});const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.08});document.querySelectorAll('.reveal').forEach((el,index)=>{el.style.transitionDelay=`${Math.min(index%8*25,175)}ms`;observer.observe(el)});document.addEventListener('keydown',event=>{if(event.key==='Escape'){document.querySelectorAll('.floating.open,.time-panel.open').forEach(el=>el.classList.remove('open'));const overlay=[...document.querySelectorAll('.overlay.open')].pop();if(overlay)closeOverlay(overlay.id)}})})
function togglePanel(id){document.getElementById(id)?.classList.toggle('open')}
function openOverlay(id){document.getElementById(id)?.classList.add('open');document.body.style.overflow='hidden'}
function closeOverlay(id){document.getElementById(id)?.classList.remove('open');if(!document.querySelector('.overlay.open'))document.body.style.overflow=''}
function backdrop(event){if(event.target===event.currentTarget)closeOverlay(event.currentTarget.id)}
function flash(id,duration=1800){const el=document.getElementById(id);el?.classList.add('show');setTimeout(()=>el?.classList.remove('show'),duration)}
function setActive(button,selector='button'){button.parentElement.querySelectorAll(selector).forEach(item=>item.classList.toggle('active',item===button))}
function setTime(value){document.querySelectorAll('[data-time-value]').forEach(el=>el.textContent=value);document.querySelectorAll('.time-column button').forEach(el=>el.classList.toggle('selected',value.split(':').includes(el.textContent)));document.querySelector('.time-panel')?.classList.remove('open')}
